<html>
    <head>
        <meta http-equiv="content-type" content="text/html; charset=utf-8" />
        <script language="JavaScript" type="text/javascript" src="{{constant('pokemon\\Bootstrap::ENTRY_URL')}}js/jquery-3.2.1.min.js"></script>
        <script language="JavaScript" type="text/javascript" src="{{constant('pokemon\\Bootstrap::ENTRY_URL')}}js/pokemon.js"></script>
        <link href="{{constant('pokemon\\Bootstrap::ENTRY_URL')}}css/pokemon.css" rel="stylesheet" type="text/css" />
        <title>新規登録</title>
    </head>
    <body>
        <p>新しいポケモンを登録する</p>
            <form method="post" action="index.php">
                <table>
                    <tr>
                        <th>なまえ<span class="red">*</span></th>
                        <td>
                            <input type="text" name="pokemon_name" value="{{dataArr.pokemon_name}}" />
                            {% if errArr.pokemon_name | length > 0 %}<br><span class="red">{{errArr.pokemon_name}}</span>{% endif %}
                        </td>
                    </tr>
                    <tr>
                        <th>せつめい<span class="red">*</span></th>
                        <td>
                            <textarea name="detail" rows="4" cols="30">{{dataArr.detail}}</textarea>
                            {% if errArr.detail | length > 0 %}<br><span class="red">{{errArr.detail}}</span>{% endif %}
                        </td>
                    </tr>
                    <tr>
                        <th>HP<span class="red">*</span></th>
                        <td>
                            <input type="number" name="hp" value="{{dataArr.hp}}" />
                            {% if errArr.hp | length > 0 %}<br><span class="red">{{errArr.hp}}</span>{% endif %}
                        </td>
                    </tr>
                    <tr>
                        <th>タイプ1<span class="red">*</span></th>
                        <td>
                            <select name="type_id">
                                {% for index, label in typeArr %}
                                    <option value="{{index}}" {% if dataArr.type_id == index %} selected {% endif %}>{{label}}</option>
                                {% endfor %}
                            </select>
                            {% if errArr.type_id | length > 0 %}<br><span class="red">{{errArr.type_id}}</span>{% endif %}
                        </td>
                    </tr>
                    <th>タイプ2</th>
                        <td>
                            <select name="type_id2">
                                {% for index, label in typeArr %}
                                    <option value="{{index}}" {% if dataArr.type_id2 == index %} selected {% endif %}>{{label}}</option>
                                {% endfor %}
                            </select>
                            {% if errArr.type_id2 | length > 0 %}<br><span class="red">{{errArr.type_id2}}</span>{% endif %}
                        </td>
                    </tr>
                </table>
                <p><a href="{{constant('pokemon\\Bootstrap::ENTRY_URL')}}index.php?display=back">戻る</a></p>
                <div><input type="submit" name="confirm" value="登録確認" /></div>
             </form>
    </body>
</html>